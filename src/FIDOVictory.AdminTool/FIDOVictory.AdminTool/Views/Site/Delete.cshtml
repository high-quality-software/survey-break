@model FIDOVictory.AdminTool.Models.Site.SiteModel

@{
    ViewBag.Title = "Delete Site";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
        @using (Html.BeginForm("Delete", "Site", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            <div class="form-group">
                <label for="SiteID" class="control-label col-md-3">Site ID:</label>
                <div class="col-md-9">
                    @Html.TextBoxFor(model => model.SiteID, new { @class = "form-control", @readonly = "readonly" })
                </div>
            </div>
            <div class="form-group">
                <label for="Name" class="control-label col-md-3">Name:</label>
                <div class="col-md-9">
                    @Html.TextBoxFor(model => model.Name, new { @class = "form-control", @readonly = "readonly" })
                </div>
            </div>
            <br />
            <div class="form-group">
                <div class="col-md-offset-3 col-md-9 centered">
                    <div class="alert alert-warning ">
                        <strong>Are you sure want to delete this site?</strong>
                    </div>
                </div>
                <div class="col-md-offset-3 col-md-9">
                    <div class="pull-right">
                        <a href="@Url.RouteUrl("ListSites")" class="btn btn-default">Cancel</a>
                        <button type="submit" class="btn btn-primary">Yes</button>
                    </div>
                </div>
            </div>

            <br />
            <br />
            <br />
            <br />

        }
    </div>
    <div class="col-md-2"></div>
</div>
